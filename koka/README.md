koka unikernel hello world

Koka is a strongly typed functional-style language with effect types and
handlers.

You can compile:

```
    koka fibonacci.kk
```

```
    ops run ./.koka/v3.2.2/gcc-debug-55adbc/fibonacci__main
```
