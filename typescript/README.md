Create it:

```
npm init --yes
npm install express
npm i -D typescript @types/express @types/node
```

Run it:

```
➜ ops pkg load eyberg/node:v16.5.0 -p 3000 -c config.json
booting /Users/eyberg/.ops/images/node ...
en1: assigned 10.0.2.15
en1: assigned FE80::8848:35FF:FE44:557F
The application is listening on port 3000!
```
