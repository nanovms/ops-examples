minc unikernel hello world

A minimal language for building native software
Compiles directly to native executables for Windows, Linux, macOS, iOS,
Android, and WebAssembly. No assembler, no linker, no runtime.


```
    minc hello.mc
    ops run hello
```
